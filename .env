# API Base URL (optional - will fallback to localStorage if unavailable)
# For local development with JSON-server:
VITE_API_BASE_URL=http://localhost:5000

# For production (GitHub Pages), leave empty or comment out to use localStorage:
# VITE_API_BASE_URL=
